global:
  a2a_server:
    host: "0.0.0.0"
    port: 9090
    preferred_transport: "json-rpc"  # Use JSON-RPC transport
llms:
  main-llm:
    type: "openai"
    model: "gpt-4o-mini"
    api_key: "${OPENAI_API_KEY}"
tools:
  weather:
    type: "mcp"
    enabled: true
    server_url: "https://apollo.composio.dev/v3/mcp/91f6d171-bd21-4beb-a513-750e8809a5a2/mcp?user_id=hector"
agents:
  weather_assistant:
    name: "Weather Assistant"
    description: "A joyful weather assistant"
    llm: "main-llm"
    prompt:
      system_prompt: "You are a helpful, joyful weather assistant with access to real-time weather data. When users ask about weather, use the weather tool to get accurate information. Be cheerful and helpful!"

