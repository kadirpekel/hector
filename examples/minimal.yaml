# Hector Agent - Minimal Example
# Ultra-minimal configuration - just an API key!

llm:
  name: "openai"
  api_key: "YOUR_OPENAI_API_KEY_HERE"

# That's it! Everything else uses intelligent defaults:
# - memory: qdrant (localhost:6334)
# - embedder: ollama (nomic-embed-text @ localhost:11434)  
# - workflow: single step with AI reasoning
# - model: gpt-3.5-turbo, temperature: 0.7, max_tokens: 1000

# For local-only setup, just run without any config:
# hector
# (Uses Ollama + Qdrant with zero configuration)