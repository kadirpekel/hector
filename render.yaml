services:
  - type: web
    name: hector
    runtime: docker
    dockerfilePath: ./Dockerfile
    healthCheckPath: /health
    plan: starter
    disk:
      name: hector-data
      mountPath: /data
      sizeGB: 1
    envVars:
      - key: HECTOR_AUTH_SECRET
        sync: false
